//dichiarazioni costanti
const N = prompt("inserisci un numero");

let i = 0;

while (i == N) {

    let y = 0

    while (y == 10) {
        const array = [];
        const numero = Math.floor(Math.random() * 100) + 1;
        array.push(numero);

        y++;
    }

    console.log[array];
    i++


}