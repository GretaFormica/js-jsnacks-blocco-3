//array vuoto
const array = [];
let numero;

let sum = 0;

while (sum < 50) {
    prompt("inserisci un numero");
    numero = prompt("inserisci un numero");
    array.push(mumero);
    sum = sum + numero
    console.log(array)
}